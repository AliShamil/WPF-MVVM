<Window x:Class="WPF_MVVM.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_MVVM.Views"
        mc:Ignorable="d"
        Title="MainView" Height="442" Width="800">
    <!--<StackPanel>
        <TextBox></TextBox>
        <Button
            
            Command="{Binding ShowCommand}"
            CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
            Content="Show">
            
        </Button>
    </StackPanel>-->

    <StackPanel HorizontalAlignment="Center">
        <ComboBox></ComboBox>
        <TextBlock Text="Make"/>
        <TextBlock Text="{Binding Car.Make}"/>
        <TextBlock Text="Model"/>
        <TextBlock Text="{Binding Car.Model}"/>
        <TextBlock Text="Year"/>
        <TextBlock Text="{Binding Car.Year}"/>

        <StackPanel Orientation="Horizontal">
            <Button Command="{Binding ShowCommand}" Content="Show"/>
            <Button Command="{Binding ShowCommand}" Content="Add"/>
            <Button Command="{Binding ShowCommand}" Content="Update"/>
            <Button Command="{Binding ShowCommand}" Content="Delete"/>
        </StackPanel>
    </StackPanel>
    
</Window>
